name: flutter_firebase_camera_challenge
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.1 <4.0.0"

dependencies:
  better_open_file: ^3.6.5
  camerawesome: ^1.4.0
  cloud_firestore: ^4.4.5
  firebase_core: ^2.3.0
  firebase_storage: ^11.0.16
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.10
  path_provider: ^2.0.11
  riverpod: ^2.3.6

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
